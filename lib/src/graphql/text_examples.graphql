# import './fragments/text_processing.graphql'

query textExamples ($offset: Int!, $max: Int!, $searchterm: String!, $lang: LangType!, $source: String, $sensitive: Boolean!) {
	textsearch (offset: $offset, max: $max, searchterm: $searchterm, lang: $lang, source: $source, sensitive: $sensitive) {
		texts {
			... on Example {
				text {
						...TextProcessing
				}
				translations {
					text {
							...TextProcessing
					}
				}
			}
		}
	}
}